{
  "name": "spectra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "build:debug": "cargo build",
    "test": "cargo test",
    "lint": "cargo clippy -- -D warnings",
    "fmt": "cargo fmt",
    "clean": "cargo clean"
  }
}
